# yaml-language-server: $schema=../../schemas/fragment.schema.json
schema_version: 1
token_cost:
  claude: 0
  codex: 132
  copilot: 0
id: code-is-authoritative
description: Code is the only source of truth; a code file carries no comment and no docstring.
category: project
priority: high
applies_to: [all]
tags:
- quality
- style
status: active
scope:
  paths:
  - src/**
  - tests/**
  - packages/**
  - site/**
  - .scripts/**
  - .basicly/core/hooks/**
  - .basicly/core/kit/**
title: Code Is Authoritative
body: |
  **A code file carries no prose.** No comment, no docstring: the code is the only source
  of truth, so changing it cannot leave a stale claim beside it. `no-comments` refuses the
  commit; `.basicly/core/kit/comments/cli.py fix <path>` removes them.

  A **directive a tool reads** stays — `noqa`, `nosec`, `type: ignore`, `pragma: no cover`,
  a shebang, `fmt: off`, a licence banner.

  Put the fact a comment would have carried where it stays true: a name, a test name, the
  tracker record, a README. Needing prose to say *what* the code does is a defect in the code.
