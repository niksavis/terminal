# yaml-language-server: $schema=../../schemas/fragment.schema.json
schema_version: 1
id: self-improvement-retro
description: End-of-session retrospective proposing concrete, approvable fixes to this repo's own instructions/skills/hooks.
category: project
priority: medium
applies_to:
- all
tags:
- retro
- self-improvement
status: active
body: |
  - When a session hits a real rejection or user-corrected mistake, run the retro from the `session-finish` skill: find the root cause and propose the exact fragment/skill/hook change that would have prevented it — never a vague "be careful", never self-applied. Skip it when nothing concrete surfaced.
