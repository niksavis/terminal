# yaml-language-server: $schema=../../schemas/fragment.schema.json
schema_version: 1
token_cost:
  claude: 43
  codex: 0
  copilot: 0
id: claude-defaults
description: Claude Code file locations.
category: project
priority: medium
applies_to: [claude]
tags:
- claude
status: active
title: Claude Notes
body: |
  - `.claude/skills/` and `.claude/rules/*.md` are generated. Edit their catalog sources.
  - Put a Claude-only prompt, command or runbook in `.claude/*.md`.
