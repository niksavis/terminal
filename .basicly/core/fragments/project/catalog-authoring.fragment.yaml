# yaml-language-server: $schema=../../schemas/fragment.schema.json
schema_version: 1
id: catalog-authoring
description: How catalog content (skills and fragments) is authored and projected.
category: project
priority: medium
applies_to: [all]
tags: [catalog, authoring]
status: active
body: |
  - Author catalog content as YAML sources, never a discoverable `.md`; scaffold with `basicly catalog new skill`/`catalog new fragment` and follow the `catalog-authoring` skill — `basicly catalog lint` enforces the format.
