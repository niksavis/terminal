## Tracker Board

**`python3 .basicly/kit/board/server.py serve .basicly/ledger` serves the backlog as a page
and an HTTP API on `127.0.0.1:8765`.** A story saved on the page carries the label `refine`
and is not ready until an agent does the refinement pass. See the `tracker-board` skill.
