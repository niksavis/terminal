# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/
*.egg-info/
dist/
build/

# uv
.uv/

# IDEs
# .vscode/ is intentionally tracked in this repo so workspace settings are shared.
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Test artifacts
.pytest_cache/
.coverage
htmlcov/

# Ruff
.ruff_cache/

# Local environment
.env
.env.local

# Claude Code per-machine settings (and its atomic-write temp files)
.claude/settings.local.json*

# Node.js
node_modules/
# package.json and package-lock.json are tracked to pin markdownlint-cli2.
# Per-machine basicly overrides; harness keys win over basicly.toml.
basicly.local.toml
# Derived folds of the committed event log. The log is the truth; never commit a fold.
.basicly/ledger/snapshot.jsonl
.basicly/ledger/checkpoint-*.jsonl
# Your copy of a file basicly replaced; delete it once you have merged what you want.
*.basicly-bak
