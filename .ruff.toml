target-version = "py314"
line-length = 100
indent-width = 4

# .basicly/core is vendored upstream code, replaced wholesale by `basicly
# install`. Linting it reports defects we cannot fix and that the next install
# would undo: 0.12.0's hooks carry `# noqa: BLE001` for a rule this config does
# not select, so RUF100 failed the verify gate on files we do not own.
extend-exclude = [".basicly/core"]

[lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "D",   # pydocstyle
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
    "ARG", # flake8-unused-arguments
    "PTH", # flake8-use-pathlib
    "ERA", # eradicate
    "PL",  # Pylint
    "RUF", # Ruff-specific rules
]
ignore = [
    "D100",   # Missing docstring in public module
    "D104",   # Missing docstring in public package
    "D203",   # 1 blank line required before class docstring
    "D213",   # Multi-line docstring summary should start at the second line
    "PLR2004", # Magic value used in comparison (common in tests and small scripts)
]

[lint.pydocstyle]
convention = "google"

[format]
preview = true
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"
